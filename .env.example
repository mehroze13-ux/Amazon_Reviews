# HuggingFace API token (free at https://huggingface.co/settings/tokens)
# Without this token, tagging falls back to VADER (local, no download needed)
HF_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Dashboard port
PORT=8000
